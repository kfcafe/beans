---
id: '89.2'
title: 'Vibecheck: Improve src/commands/run.rs'
slug: vibecheck-improve-srccommandsrunrs
status: open
priority: 2
created_at: 2026-02-27T04:45:11.185005Z
updated_at: 2026-02-27T04:45:11.185005Z
labels:
- vibecheck
parent: '89'
verify: cargo test
tokens: 19348
tokens_updated: 2026-02-27T04:45:11.186378Z
---

## Task
Improve `src/commands/run.rs` based on vibecheck findings.

## Findings (top 20)
- [high] function-length — `src/commands/run.rs:915-1340`
  - `run_ready_queue_direct` is 130+ lines of imperative orchestration with a main loop containing channel-based concurrency, dependency tracking, ready-bean selection, thread spawning, and result collection all inline. `run_single_direct` below it is another 180+ lines handling file locking, command building, process monitoring, and result construction.
  - Suggested fix: Extract `run_single_direct` into its own module or at least split the pi command construction, process monitoring, and result assembly into separate functions. The ready-queue orchestrator would benefit from a `ReadyQueue` struct encapsulating state.
- [medium] dead-code — `src/commands/run.rs:445-449`
  - `run_loop` accepts `_spawn_mode: &SpawnMode` as a parameter (prefixed with `_` to suppress warnings) but never uses it — it recalculates `spawn_mode` inside the loop body via `determine_spawn_mode(&config)`. The parameter is dead.
  - Suggested fix: Remove the `_spawn_mode` parameter from `run_loop` and its call site in `cmd_run`.
- [medium] architecture — `src/commands/run.rs:1-2216`
  - `run.rs` at 2,216 lines contains: dispatch planning, wave computation, two completely separate execution backends (template mode and direct mode), ready-queue orchestration, process monitoring, context assembly, JSON stream formatting, and ~700 lines of tests. It's the second-largest file and growing.
  - Suggested fix: Split into modules: `run/plan.rs` (dispatch planning, wave computation), `run/template.rs` (legacy template execution), `run/direct.rs` (pi direct mode), `run/queue.rs` (ready-queue orchestrator), `run/mod.rs` (public `cmd_run` entry point that delegates).
- [low] consistency — `src/commands/run.rs:40-47`
  - `BeanAction` in `run.rs` and `AgentAction` in `spawner.rs` are identical enums (`Implement` / `Plan`) with the same Display impl. They exist because `run.rs` uses `SizedBean` with `BeanAction` while `spawner.rs` uses `AgentProcess` with `AgentAction`. This duplication means changes to action types must be made in two places.
  - Suggested fix: Define a single `BeanAction` enum in a shared location (e.g., `bean.rs` or a new `types.rs`) and use it in both modules.

## Notes
- Keep behavior the same unless the finding explicitly calls for a behavior change.
- Prefer small, focused changes.

Snapshot: ac9a6079-2c27-48d7-ad0b-abc10f3ed205
