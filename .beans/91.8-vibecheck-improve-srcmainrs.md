---
id: '91.8'
title: 'Vibecheck: Improve src/main.rs'
slug: vibecheck-improve-srcmainrs
status: open
priority: 2
created_at: 2026-02-27T04:45:57.905655Z
updated_at: 2026-02-27T04:45:57.905655Z
labels:
- vibecheck
parent: '91'
verify: cargo test
tokens: 6010
tokens_updated: 2026-02-27T04:45:57.906648Z
---

## Task
Improve `src/main.rs` based on vibecheck findings.

## Findings (top 20)
- [medium] function-length — `src/main.rs:46-695`
  - The `main()` function is 650 lines — almost entirely a single `match cli.command` block that dispatches 35+ commands. While each arm is straightforward, the sheer length makes it hard to navigate and the `Command::Create` arm alone is ~170 lines with nested logic for interactive mode, stdin resolution, `--run` flag, and `create next` subcommand handling.
  - Suggested fix: Extract the `Command::Create` arm into a dedicated `handle_create(beans_dir, ...)` function. Similarly, extract any arms with >20 lines of logic (Create, Update, Quick, Plan). The main match should ideally be a one-liner dispatch per command.

## Notes
- Keep behavior the same unless the finding explicitly calls for a behavior change.
- Prefer small, focused changes.

Snapshot: e19ec8a8-3370-4fa6-a0f8-a1d592485e04
